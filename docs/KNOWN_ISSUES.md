# Known issues — moved

This document is **development history**, not a list of open issues, and it does
not ship. It now lives at **[history/KNOWN_ISSUES.md](history/KNOWN_ISSUES.md)**.

This stub exists only because eight `include/xp/*.hpp` headers cite the old path
in comments, and the library sources are not edited by measurement work. Fold it
away the next time those comments are touched for another reason.

- **Open defects, enforced:** `validation/sweep/open_defects.txt`, checked in both
  directions by the `sweep_absolute_gate` ctest target.
- **How correctness is measured:** [CORRECTNESS.md](CORRECTNESS.md).
